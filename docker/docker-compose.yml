version: '3.8'

services:
  ros2-dev:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    volumes:
      - ..:/workspace
    environment:
      - DISPLAY=host.docker.internal:0
    ports:
      - "8080:8080"
    stdin_open: true
    tty: true
